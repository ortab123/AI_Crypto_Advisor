# Backend API base URL (used in all fetch calls at runtime)
VITE_API_URL=http://localhost:3001/api

# Dev server proxy target — must match backend PORT (used in vite.config.ts)
VITE_BACKEND_URL=http://localhost:3001

# Dev server port
VITE_PORT=5173
